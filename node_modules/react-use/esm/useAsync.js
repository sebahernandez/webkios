import { useEffect } from 'react';
import useAsyncFn from './useAsyncFn';
var useAsync = function (fn, deps) {
    if (deps === void 0) { deps = []; }
    var _a = useAsyncFn(fn, deps), state = _a[0], callback = _a[1];
    useEffect(function () {
        callback();
    }, [callback]);
    return state;
};
export default useAsync;
